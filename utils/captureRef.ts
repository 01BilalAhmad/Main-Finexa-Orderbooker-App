// Native (Android/iOS): capture a view as image using react-native-view-shot
import { captureRef as rnCaptureRef } from 'react-native-view-shot';

export { captureRef } from 'react-native-view-shot';
